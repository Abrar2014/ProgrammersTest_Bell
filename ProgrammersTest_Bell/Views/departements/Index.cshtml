@model IEnumerable<ProgrammersTest_Bell.Models.departement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomDepartement)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomDepartement)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDepartement }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDepartement }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDepartement })
        </td>
    </tr>
}

</table>
