@model IEnumerable<ProgrammersTest_Bell.Models.employe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.departement.nomDepartement)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.departement.nomDepartement)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEmploye }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEmploye }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEmploye })
        </td>
    </tr>
}

</table>
